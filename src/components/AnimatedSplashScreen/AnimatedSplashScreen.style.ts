import { StyleSheet } from "react-native";

export default StyleSheet.create({
  main: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
  lottieSize: {
    width: "20%",
    maxWidth: 200,
  },
});
